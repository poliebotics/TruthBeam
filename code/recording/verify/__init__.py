"""Truth Beam v9 verifier package.

Public entry point: `verify_v9.verify_session(session_dir, online=False)`.

Also runnable as a script:
    python3 -m truth_beam_recording.verify.verify_v9 --session-dir <path>
"""
